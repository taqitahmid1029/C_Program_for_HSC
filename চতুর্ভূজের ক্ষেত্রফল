#include<stdio.h>

int main()
{
    float l, w, area;
    printf("enter length and width of a rectangle: ");
    scanf("%f %f", &l, &w);
    area=l*w;
    printf("area of the rectangle is %.2f", area);
    return 0;
}
