#include<stdio.h>

int main()
{
    float b, h, area;
    printf("enter base and height of a parallelogram: ");
    scanf("%f %f", &b, &h);
    area=b*h;
    printf("area of the parallelogram is %.2f", area);
    return 0;
}
