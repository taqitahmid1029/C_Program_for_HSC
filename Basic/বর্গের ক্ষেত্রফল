#include<stdio.h>
#include<math.h>

int main()
{
    float a, area;
    printf("Enter side of a square: ");
    scanf("%f", &a);
    area=pow(a, 2);
    printf("Area of the square is %.2f", area);
    return 0;
}
