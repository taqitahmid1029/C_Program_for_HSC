#include<stdio.h>
#include<math.h>
#define pi=3.1416

int main() {
    float r, area;
    printf("Enter radius: ");
    scanf("%f", &r);
    area=pi*pow(r,2);
    printf("Area is %.2f", area);
    return 0;
}
