#include<stdio.h>

int main()
{
    int a, b;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    if(a<b)
        printf("%d is smallest numner", a);
    else
        printf("%d is smallest number", b);
    return 0;
}
